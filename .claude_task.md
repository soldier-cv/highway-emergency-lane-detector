# Task: Highway Emergency Lane Detector - Project Optimization

Project: D:\dev\agent-repos\highway-emergency-lane-detector
Fork: https://github.com/soldier-cv/highway-emergency-lane-detector

## Context
Emergency lane violation detection system using YOLOv8 + HyperLPR3 + OpenVINO GPU.
Key files:
- `emergency_lane/run_gpu_v8.py` (807 lines) - CLI detection
- `emergency_lane/traffic_violation_gui.py` (1046 lines) - GUI detection  
- `emergency_lane/lpr3_openvino.py` (328 lines) - License plate recognition via OpenVINO
- `emergency_lane/utils.py` (47 lines) - Shared utilities
- `web_server.py` (488 lines) - Flask web service
- `start_web.pyw` - Web launcher (no console)

## Tasks (execute ALL in order)

### 1. Create unified model config
Create `emergency_lane/models/__init__.py` and `emergency_lane/models/config.py`:
- Define all model paths relative to project root
- YOLO model: currently `yolov8s_openvino_model/` in project root
- HyperLPR3 models: currently at `~/.hyperlpr3/20230229/onnx/` (det_320.onnx, det_640.onnx, rec.onnx, cls.onnx)
- Resolution: prefer project-local `models/` dir, fallback to existing paths
- Update all files to import from this config

### 2. Create requirements.txt
ultralytics, openvino, opencv-python-headless, numpy, flask, torch

### 3. Create model download script `setup_models.py`
- Download HyperLPR3 ONNX models to `models/onnx/` (from GitHub releases or ModelScope)
- Guide user for YOLO model placement
- Print clear instructions

### 4. Remove JSON report output
- Remove `json.dump` / `*_violation_report.json` from run_gpu_v8.py
- Remove JSON output from traffic_violation_gui.py
- Keep HTML report only

### 5. Refactor common detection logic into `emergency_lane/detector.py`
Extract shared code: model loading, detection loop, plate recognition, violation judgment, screenshot saving, FFmpeg clip generation.
Update run_gpu_v8.py and traffic_violation_gui.py to import from detector.py.
Keep CLI args and GUI functionality unchanged.

### 6. Update README.md
- Installation (pip install -r requirements.txt + python setup_models.py)
- Usage (CLI / GUI / Web)
- Project structure
- Model version notes (YOLOv8s for accuracy, v8n for speed)

### 7. Git setup and push
- git init
- git remote add origin https://github.com/soldier-cv/highway-emergency-lane-detector.git
- git add -A
- git commit -m "refactor: unified model config, remove JSON output, add setup script"
- git push -u origin main --force

## Constraints
- Keep all existing functionality working
- Windows paths: use raw strings r''
- Do NOT modify .gitignore (already correct)
- Chinese comments are fine, keep them
- Do NOT delete working code, only refactor
