# 高速公路应急车道违章检测系统 - 依赖
# 核心
opencv-python>=4.8.0
numpy>=1.24.0
ultralytics>=8.0.0

# GPU加速 (NVIDIA CUDA)
torch>=2.0.0
onnxruntime-gpu>=1.17.0

# GPU加速 (Intel OpenVINO, 备选)
openvino>=2024.0.0

# 工具
Pillow>=10.0.0
pyyaml>=6.0

# Web界面 (可选)
flask>=3.0.0
